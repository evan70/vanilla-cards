# Vanilla Cards Design System

BEM CSS card components — no framework dependencies.

## Philosophy

Structure is shared, style is yours.

- Pure CSS — no React, no Vue, no Svelte
- BEM naming convention
- CSS custom properties (tokens)
- Dark/light theme support
- PHP template friendly
